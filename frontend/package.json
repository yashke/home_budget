{
  "name": "home_budget_frontend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "webpack --progress --profile --colors --bail",
    "dev-server": "npm run build && webpack-dev-server --hot --inline --progress --colors --content-base public/ --host 0.0.0.0"
  },
  "author": "Jan Filipowski",
  "license": "MIT",
  "devDependencies": {
    "webpack": "^2.2.1",
    "webpack-dev-server": "^2.4.1"
  }
}
